let heading = document.getElementById("heading");
console.log(heading)

let subhead = document.getElementsByClassName("sub-head")
console.log(subhead);
console.dir(subhead)

let firstelements = document.querySelector("p");
console.dir(firstelements)

// // this document.queryselctor means it will select only first element
// // this document.queryselctorall means it will select only all elements
// // this is for tags

let allelements = document.querySelectorAll("li");
console.dir(allelements);

//this one is for clasess we should add "." to each class to access elements

let classes =  document.querySelector(".sub-head");
console.dir(sub-head);

// this one is for id we have to use # for id to access elementss
let container =  document.querySelector("#container");
console.dir(container)

document.querySelector("div")
document.querySelector("div").children;
